<h1>Listing posts</h1>

<table>
  <tr>
    <th>Newreleases</th>
    <th>Mostpopular</th>
    <th>Commingsoon</th>
    <th>Games</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.NewReleases %></td>
    <td><%= post.MostPopular %></td>
    <td><%= post.commingsoon %></td>
    <td><%= post.games %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path %>
